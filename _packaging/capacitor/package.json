{
  "name": "atlas3d-capacitor",
  "private": true,
  "version": "0.1.0",
  "description": "Powloka Android (Capacitor) dla atlasu anatomicznego 3D.",
  "scripts": {
    "assemble": "node ../assemble.mjs",
    "sync": "node ../assemble.mjs && cap sync android",
    "open": "cap open android",
    "apk": "node ../assemble.mjs && cap sync android && cd android && gradlew.bat assembleRelease"
  },
  "devDependencies": {
    "@capacitor/cli": "^7"
  },
  "dependencies": {
    "@capacitor/core": "^7",
    "@capacitor/android": "^7",
    "@capacitor/browser": "^7",
    "@capgo/capacitor-updater": "^7"
  }
}
